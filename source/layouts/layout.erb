<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <meta http-equiv="x-ua-compatible" content="ie=edge">
  <meta name="viewport"
  content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <!-- Use the title from a page's frontmatter if it has one -->
  <title><%= current_page.data.title || "Middleman" %></title>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
  <%= stylesheet_link_tag "site" %>
  <%= javascript_include_tag "site" %>
</head>
<body>
  <%= yield %>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/1.20.3/TweenMax.min.js"></script>

  <script type="text/javascript">

    const tl = new TimelineLite();
    const i1 = document.getElementById("img1");
    const i2 = document.getElementById("img2");
    const i3 = document.getElementById("img3");
    const i4 = document.getElementById("img4");
    const s1 = document.getElementById("slider1");
    const s2 = document.getElementById("slider2");
    const s3 = document.getElementById("slider3");
    const s4 = document.getElementById("slider4");
    const s5 = document.getElementById("slider5");

    tl.from(i1, 1, {autoAlpha:0, delay:5})
    .from(s1, 0.5, {x: '-=200px', autoAlpha: 0})
    .from(s2, 0.5, {x: '+=200px', autoAlpha: 0})
    .from(s1, 0.5, {x: '-=400px', autoAlpha: 0})
    .from(s2, 0.5, {x: '+=400px', autoAlpha: 0})
    .from(i2, 1, {autoAlpha:0, delay:5})
    .from(i3, 1, {autoAlpha:0, delay:5})
    .from(i4, 1, {autoAlpha:0, delay:5});
    // .from("#slider3", 2, {x: '-=200px', autoAlpha: 0})
    // .from("#slider4", 2, {x: '+=200px', autoAlpha: 0})
    // .from("#slider3", 2, {x: '-=400px', autoAlpha: 0})
    // .from("#slider4", 2, {x: '+=400px', autoAlpha: 0})
    // .from("#slider5", 2, {x: '-=200px', autoAlpha: 0})
    // // .from("#slider5", 3, {x: '-=400px', autoAlpha: 0})
    // const images = document.body.querySelectorAll('img');
    // const timeline = new TimelineMax({repeat:-1});
    // timeline.append(TweenMax.staggerTo(images, 1, {css:{autoAlpha:1}, repeat:1, yoyo:true, repeatDelay:.5}, 2.5));

//     var tl = new TimelineLite({onUpdate:updateSlider});
//     tl.set("#content", {visibility:"visible"})
//   .from("h1", 0.5, {left:100, autoAlpha:0}) // autoAlpha handles both css properties visibility and opacity.
// .from("h2", 0.5, {left:-100, autoAlpha:0}, "-=0.25") //add tween 0.25 seconds before previous tween ends
// .from("#feature", 0.5, {scale:0.5, autoAlpha:0}, "feature") // add feature label at start position of this tween
// .from("#description", 0.5, {left:100, autoAlpha:0}, "feature+=0.25") // add tween 0.25 seconds after the feature label
// .staggerFrom("#nav img", 0.5, {scale:0, rotation:-180, autoAlpha:0}, 0.2, "stagger");


    // TweenLite.from(slide1, 2, {x: '-=200px', autoAlpha: 0});
    // TweenLite.to(slide1, 2, {autoAlpha: 0, delay: 2});
    // TweenLite.from(slide2, 2, {x: '+=200px', autoAlpha: 0, delay: 2.5});
    // TweenLite.to(slide2, 2, {autoAlpha: 0, delay: 4.5});
    const $box = $('#box');
    TweenLite.set($box, {x: '-=200px', scale: 0.3});
    TweenLite.set($box, {x: '+=100px', scale: 0.6, delay: 1});
    TweenLite.set($box, {x: '-50%', scale: 1, delay: 2});

  </script>
</body>
</html>
